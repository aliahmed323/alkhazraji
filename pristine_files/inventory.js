"/**\n * inventory.js — Inventory Page Module\n * Exposes: window.InventoryPage = { init, refresh }\n */\n(function () {\n  'use strict';\n\n  /* ───────────────────────────── state ───────────────────────────── */\n  let currentFilter = 'today';\n  let purchasesData = [];\n  let salesData = [];\n\n  /* ───────────────────────────── helpers ─────────────────────────── */\n  function getDateRange(filter) {\n    const now = new Date();\n    let start, end;\n    end = new Date(now.getFullYear(), now.getMonth(), now.getDate(), 23, 59, 59, 999);\n\n    switch (filter) {\n      case 'today':\n        start = new Date(now.getFullYear(), now.getMonth(), now.getDate(), 0, 0, 0, 0);\n        break;\n      case 'week': {\n        const day = now.getDay();\n        const diff = (day === 0 ? -6 : 1 - day);\n        start = new Date(now);\n        start.setDate(now.getDate() + diff);\n        start.setHours(0, 0, 0, 0);\n        break;\n      }\n      case 'month':\n        start = new Date(now.getFullYear(), now.getMonth(), 1, 0, 0, 0, 0);\n        break;\n      case 'all':\n        start = new Date(2000, 0, 1);\n        end = new Date(2100, 0, 1);\n        break;\n      default:\n        start = new Date(now.getFullYear(), now.getMonth(), now.getDate(), 0, 0, 0, 0);\n    }\n    return { start, end };\n  }\n\n  function filterByDate(items, filter) {\n    const { start, end } = getDateRange(filter);\n    return items.filter(item => {\n      const d = item.date ? new Date(item.date) : (item.createdAt ? item.createdAt.toDate ? item.createdAt.toDate() : new Date(item.createdAt) : new Date());\n      return d >= start && d <= end;\n    });\n  }\n\n  function calcInventory(purchases, sales) {\n    const FISH_TYPES = window.FISH_TYPES || ['شبوط', 'كطان', 'بني', 'گطان', 'مشهوف', 'سمك 
<truncated 17255 bytes>