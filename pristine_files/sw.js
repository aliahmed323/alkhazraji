"/**\n * sw.js — Service Worker for سمكنا PWA\n * Strategy:\n *   - Cache-first for all static assets\n *   - Network-first for Firebase/API requests\n *   - Offline fallback to cached index.html\n */\n\nconst CACHE_NAME    = 'samakna-v1';\nconst DYNAMIC_CACHE = 'samakna-dynamic-v1';\n\n/* ─────────────────────────── Assets to pre-cache ─────────────────────────── */\nconst ASSETS_TO_CACHE = [\n  './',\n  './index.html',\n  './css/main.css',\n  './css/components.css',\n  './js/utils.js',\n  './js/db.js',\n  './js/app.js',\n  './js/dashboard.js',\n  './js/sales.js',\n  './js/purchases.js',\n  './js/expenses.js',\n  './js/suppliers.js',\n  './js/debts.js',\n  './js/inventory.js',\n  './js/reports.js',\n  './js/settings.js',\n  './manifest.json',\n  './icons/icon-192.png',\n  './icons/icon-512.png',\n  'https://fonts.googleapis.com/css2?family=Cairo:wght@300;400;500;600;700;900&display=swap',\n  'https://unpkg.com/lucide@latest/dist/umd/lucide.min.js',\n];\n\n/* ─────────────────────────── Hosts that need Network-first ──────────────── */\nconst NETWORK_FIRST_HOSTS = [\n  'firestore.googleapis.com',\n  'firebase.googleapis.com',\n  'identitytoolkit.googleapis.com',\n  'securetoken.googleapis.com',\n  'firebaseinstallations.googleapis.com',\n  'googleapis.com',\n];\n\nfunction isNetworkFirst(url) {\n  try {\n    const hostname = new URL(url).hostname;\n    return NETWORK_FIRST_HOSTS.some(h => hostname.includes(h));\n  } catch {\n    return false;\n  }\n}\n\nfunction isStaticAsset(url) {\n  try {\n    const u = new URL(url);\n    return (\n      u.hostname === self.location.hostname ||\n      u.hostname.includes('fonts.googleapis.com') ||\n      u.hostname.includes('fonts.gstatic.com') ||\n      u.hostname.includes('unpkg.com') ||\n      u.hostname.includes('cdnjs.cloudflare.com')\n    );\n  } catch {\n    return false;\
<truncated 6305 bytes>